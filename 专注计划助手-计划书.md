# 专注计划助手｜简要计划书

## 1. 项目目标

开发一款仅供个人使用的“专注计划助手”，将任务计划、日历、番茄钟、复盘和统计放在同一套应用中。应用以 Android 手机端日常记录、Windows PC 端查看和整理为主，数据默认只保存在用户自己的设备上，不依赖账号或公共云服务。

参考截图中的清爽卡片式界面：桌面端提供顶部功能导航，移动端优先展示今日数据与统计卡片；主色可采用蓝紫色，并支持主题切换。

## 2. 使用原则

- 本地优先：默认不上传任何数据。
- 离线可用：断网时可创建任务、计时、复盘和查看已有统计。
- 数据可控：支持完整备份导出、导入恢复和 CSV 导出。
- 跨端有边界：不启用同步时，手机与 PC 各自保存数据；PC 导入手机备份后立即重新计算并展示统计。
- 可选同步：后续可接入个人 WebDAV，或在同一局域网内由电脑提供同步服务；不建设公共服务器。

## 3. 功能范围

### 3.1 任务计划

- 新建、编辑、完成、删除任务。
- 字段：标题、说明、截止日期、优先级、标签/分类、预计番茄数、完成时间。
- 按状态、优先级、标签、截止日期筛选和搜索。
- 区分“待完成”和“已完成”，为空时显示友好提示。
- 番茄钟记录可以关联到任务或分类，用于后续统计。

### 3.2 日历

- 提供月视图；需要时增加日/周视图。
- 日期格中展示当天任务数量、完成情况、专注时长与复盘状态。
- 点击日期可查看和编辑该日任务、专注记录与每日复盘。

### 3.3 番茄钟

- 支持专注、短休息、长休息三种阶段。
- 默认设置：专注 25 分钟、短休息 5 分钟、长休息 15 分钟、每 4 次专注后进入长休息；均可在设置中修改。
- 计时开始时选择关联任务或分类；支持暂停、继续、放弃、完成。
- 每次完成形成一条专注记录，保存开始/结束时间、实际时长、关联任务/分类和备注。

### 3.4 复盘

- 每日复盘：展示日期、任务完成数、专注时长/次数，并填写“今日总结”“改进建议”“明日计划”。
- 历史复盘：按日期浏览和编辑过往每日复盘。
- 周复盘与月复盘：提供固定模板，汇总本周期目标完成情况、专注数据、收获、问题与下一周期计划。

### 3.5 统计

- 概览：累计专注次数、累计专注时长、日均专注时长、当天专注次数与时长。
- 时间维度：日、周、月、年及自定义日期范围。
- 图表：
  - 按任务/分类的专注时长占比（饼图或条形图）；
  - 每日专注趋势；
  - 一天内的专注时段分布；
  - 月度和年度趋势。
- 睡眠打卡：记录入睡时间、起床时间和主观睡眠评分；统计睡眠时长趋势。跨午夜睡眠须正确计算到次日。
- 无数据时不展示空图，而是显示说明和引导操作。

### 3.6 设置与数据管理

- 番茄钟时长和轮次设置。
- 浅色/深色主题，跟随系统为可选项。
- 完整数据导出为带版本号的 JSON 文件；支持导入、预览和确认覆盖/合并策略。
- 导出任务、专注记录和睡眠记录为 CSV。
- 可选 WebDAV：填写地址、用户名、密码、远端备份文件名；提供测试连接、手动同步、拉取远端和上传本地。
- 可选局域网同步：只在同一 Wi-Fi 下启用，须有明确开关和连接提示。

## 4. 页面结构

| 页面 | 核心内容 |
| --- | --- |
| 任务计划 | 新增任务、筛选、待完成/已完成列表 |
| 日历 | 月历、每日任务和专注概览 |
| 番茄钟 | 阶段计时、任务关联、完成记录 |
| 复盘 | 每日/历史/周月复盘 |
| 统计 | 总览卡片、分类占比、趋势、时段、睡眠 |
| 设置 | 同步、计时偏好、备份导入导出、主题 |

移动端使用底部导航快速进入“任务、日历、计时、统计、我的/设置”；PC 端使用参考截图中的顶部导航。两端使用同一数据字段和统计口径。

## 5. 数据与技术建议

- 形态：响应式 PWA 网页应用，可安装到 Android 和 Windows 桌面。
- 本地数据库：IndexedDB。它适合浏览器离线存储；数据不随应用上传。
- 数据对象：任务、专注记录、每日复盘、周/月复盘、睡眠记录、用户设置、数据版本与同步元信息。
- PC 静态查看：打开网页后导入 JSON 备份，即刻在浏览器本地生成所有统计；不需要后端。
- 备份安全：导入前自动校验文件格式与版本，导入后生成一次本地备份提示；WebDAV 密码仅保留在本地浏览器中。

## 6. 分期与验收

### 第一期：可日常使用的最小版本

完成任务、月历、可关联任务的番茄钟、每日复盘、日/月基础统计、JSON 导入导出、主题与计时设置。

验收标准：在 Android 离线完成一条任务和一次番茄钟后，导出 JSON；在 Windows 浏览器导入该 JSON，任务、记录和当天统计一致。

### 第二期：完整复盘与统计

加入周/月复盘、分类占比、日/周/月/年趋势、时段统计、睡眠打卡及 CSV 导出。

验收标准：任意选择一个月，能看到该月总专注时长、日均时长、按分类占比、逐日趋势和睡眠趋势；统计结果可追溯到原始记录。

### 第三期：可选个人同步

接入 WebDAV 或局域网同步，提供冲突提示和“以本地/远端为准”的明确选择。

验收标准：两台设备在用户明确操作后可交换数据；断网、同步失败或冲突时，原有本地数据不会丢失。

## 7. 关键规则（开发前确认）

- “专注次数”按完成的专注阶段计数；中途放弃默认不计入，可在设置中调整。
- “日均专注时长”默认按有记录的自然日计算；统计页面须说明口径，并可改为按整个选定周期计算。
- 专注记录跨午夜时，时长按实际时间分摊至两个自然日；关联分类不拆分。
- 任务完成不等于已专注，二者分别统计。
- 导入默认采用“合并去重”，由每条记录的唯一 ID 和更新时间判断；覆盖本地数据必须二次确认。

## 8. 暂不纳入首版

- 多用户、账号系统、公共云端、社交功能。
- 强制系统级 App 使用时长读取（不同设备权限和平台差异较大）。
- 复杂的多人协作、共享任务和在线提醒推送。

## 9. 当前实现进度（2026-08-21）

当前源码已覆盖三期计划的大部分功能，项目进入“验证、收尾和发布维护”阶段。

### 已完成

- 第一期核心流程：任务、月历、番茄钟、每日复盘、基础统计、JSON 备份导入导出、主题与计时设置。
- 任务增强：软删除、分类管理、优先级筛选、搜索、重复任务实例生成和历史完成记录压缩。
- 第二期统计与复盘：周/月复盘汇总、分类占比、日/周/月/年/自定义统计、专注时段、睡眠打卡与趋势、CSV 和 Markdown 导出。
- 第三期同步核心：WebDAV 手动同步、远端冲突保护、ETag 前置条件、局域网 HTTPS 同步服务、历史版本查看/下载/恢复和安全合并。
- PWA 与本地数据能力：IndexedDB 持久化、离线使用、主题切换、安装提示、持久存储申请和更新提示。

### 当前验证基线

- `npm run lint`：通过。
- `npm run test -- --run`：22 个测试文件、140 个测试通过。
- `npm run build`：通过。

### 尚需完成

- 在真实 Android 和 Windows 浏览器上验证离线/PWA、备份迁移、移动端布局和长文本复盘。
- 在实际局域网环境验证证书安装、防火墙、服务启停、认证、ETag 冲突和历史恢复。
- 持续补充跨午夜、北京时间边界、空数据、重复任务归档和同步异常的回归测试。
- 设备同步水位和墓碑安全压缩仍属于后续规划；在此之前不自动物理删除墓碑。
